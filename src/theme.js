import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#17760a5",
      light: "skyblue"
    },
    secondary: {
      main: "#15c630"
    },
    otherColor: {
      main: "#333"
    }
  }
});
